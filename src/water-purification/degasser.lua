local component = require("component")
local sides = require("sides")
local event = require("event")

local MultiFluidTransposer = require("multiFluidTransposer")

-- init variables
local controller = component.get_machine


-- main loop
while true do
    
end